# Clean canonical configs

Active candidates:

- b0_deterministic.json;
- b1_select_v2_14b_nf4.json;
- retrieval.json.

The completed B1 run used Qwen 14B with runtime bitsandbytes NF4. Older 7B and
AWQ config files are historical planning artifacts and are kept under history,
not as active run defaults.
