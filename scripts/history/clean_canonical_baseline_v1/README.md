# Historical clean payload builders

The v3 and v4 wrappers are superseded by scripts/59_make_clean_payload_v5.py.
They are retained only to explain the NF4 and validator incident sequence.

The v2 wrapper remains active because v5 imports its clean-retrieval validation.
